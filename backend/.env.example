# Database Configuration
; DB_USER=postgres
; DB_PASSWORD=your_secure_password
; DB_HOST=localhost
; DB_PORT=5432
; DB_NAME=movie_platform

# JWT Secret
SECRET_KEY=your-super-secret-jwt-key-here

# Use SQLite for development (set to false for PostgreSQL)
USE_SQLITE=true